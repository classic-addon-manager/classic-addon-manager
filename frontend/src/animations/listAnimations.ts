import { fadeSlideTransition, fadeTransition } from '@/animations/transitions'

export const listAnimations = {
  container: fadeTransition,
  item: fadeSlideTransition,
  emptyState: fadeTransition,
}
